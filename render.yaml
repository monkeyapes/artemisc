services:
  - type: web
    name: artemisc-server
    env: node
    region: oregon
    plan: free
    buildCommand: cd client && npm install && npm run build && cd ../server && npm install && npx prisma generate && npm run build
    startCommand: cd server && npm run start
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: artemisc-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "3001"

databases:
  - name: artemisc-db
    region: oregon
    plan: free
    databaseName: artemisc
    user: artemisc
