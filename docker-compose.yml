services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: artemisc
      POSTGRES_USER: artemisc
      POSTGRES_PASSWORD: artemisc
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
